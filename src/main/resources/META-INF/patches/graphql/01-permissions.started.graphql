mutation{
    jcr {
        mutateNode(pathOrId: "/roles/server-administrator"){
            mutateProperty(name: "j:permissionNames") {
                addValue(value: "jobs")
            }
        }
    }
}
